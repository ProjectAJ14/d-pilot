meta {
  name: Delete Saved Query
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/saved-queries/{{savedQueryId}}
  body: none
  auth: inherit
}

docs {
  Delete a saved query you own.

  Requires a valid Bearer JWT ({{token}}).
}
