meta {
  name: Set Masked Environments
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/api/phi-config/masked-envs
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "environments": [
      "DEV",
      "QA",
      "UAT",
      "STG",
      "PROD"
    ]
  }
}

docs {
  Set the masked-environment list. Every production-like environment — any name
  containing "prod" (PROD, SUPER_PROD, PREPROD) — must remain present, or the
  request is a 422 naming the ones that were dropped.

  Values must be environments this deployment actually has (see `GET /api/config`
  → `environments`); anything else is a 400 that names the allowed values.

  Requires a valid Bearer JWT ({{token}}).
  **Admin only.**
}
