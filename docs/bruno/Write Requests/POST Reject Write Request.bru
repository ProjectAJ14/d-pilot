meta {
  name: Reject Write Request
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/write-requests/{{writeRequestId}}/reject
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "notes": "WHERE clause is too broad."
  }
}

docs {
  Reject a PENDING request.

  Requires a valid Bearer JWT ({{token}}).
  Requires **write mode** to be enabled.
}
